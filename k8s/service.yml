apiVersion: v1
kind: Service
metadata:
  name: logginginspring-service
  namespace: default
  labels:
    app: logginginspring
spec:
  type: ClusterIP
  selector:
    app: logginginspring
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP